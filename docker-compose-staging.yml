services:
  flask-app:
    image: ryanfletcher96/assignment1-flask-app:latest
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - flask-app