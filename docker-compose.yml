services:
  flask-app:
    build: .
    image: ryanfletcher96/assignment1-b00281528:latest
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask-app